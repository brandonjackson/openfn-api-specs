{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "openimis/Organization",
  "title": "Organization",
  "x-openfn-adaptor": "openimis",
  "x-source": {
    "openapi": "#/components/schemas/Organization",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "description": "openIMIS FHIR R4 Organization resource — an openIMIS policy holder, health facility or insurance organisation. The demo instance returned no instances of this resource, so only the FHIR R4 elements openIMIS documents for it are modelled; additional elements may be present.",
  "properties": {
    "resourceType": {
      "type": "string",
      "enum": [
        "Organization"
      ]
    },
    "id": {
      "type": "string",
      "description": "Logical id of the resource (an openIMIS UUID)."
    },
    "identifier": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "Business identifiers for the resource."
    }
  },
  "required": [
    "resourceType"
  ]
}
