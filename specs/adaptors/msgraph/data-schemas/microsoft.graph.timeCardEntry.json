{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msgraph/microsoft.graph.timeCardEntry",
  "title": "timeCardEntry",
  "x-openfn-adaptor": "msgraph",
  "x-source": {
    "openapi": "#/components/schemas/microsoft.graph.timeCardEntry",
    "capturedAt": "2026-09-14"
  },
  "required": [
    "@odata.type"
  ],
  "type": "object",
  "properties": {
    "breaks": {
      "type": "array",
      "items": {
        "$ref": "microsoft.graph.timeCardBreak.json"
      },
      "description": "The clock-in event of the timeCard."
    },
    "clockInEvent": {
      "anyOf": [
        {
          "$ref": "microsoft.graph.timeCardEvent.json"
        },
        {
          "type": [
            "object",
            "null"
          ]
        }
      ],
      "description": "The clock-out event of the timeCard."
    },
    "clockOutEvent": {
      "anyOf": [
        {
          "$ref": "microsoft.graph.timeCardEvent.json"
        },
        {
          "type": [
            "object",
            "null"
          ]
        }
      ],
      "description": "The list of breaks associated with the timeCard."
    },
    "@odata.type": {
      "type": "string"
    }
  }
}
