{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msgraph/microsoft.graph.synchronization",
  "title": "microsoft.graph.synchronization",
  "x-openfn-adaptor": "msgraph",
  "x-source": {
    "openapi": "#/components/schemas/microsoft.graph.synchronization",
    "capturedAt": "2026-09-14"
  },
  "allOf": [
    {
      "$ref": "microsoft.graph.entity.json"
    },
    {
      "title": "synchronization",
      "required": [
        "@odata.type"
      ],
      "type": "object",
      "properties": {
        "secrets": {
          "type": "array",
          "items": {
            "$ref": "microsoft.graph.synchronizationSecretKeyStringValuePair.json"
          },
          "description": "Represents a collection of credentials to access provisioned cloud applications."
        },
        "jobs": {
          "type": "array",
          "items": {
            "$ref": "microsoft.graph.synchronizationJob.json"
          },
          "description": "Performs synchronization by periodically running in the background, polling for changes in one directory, and pushing them to another directory.",
          "x-ms-navigationProperty": true
        },
        "templates": {
          "type": "array",
          "items": {
            "$ref": "microsoft.graph.synchronizationTemplate.json"
          },
          "description": "Preconfigured synchronization settings for a particular application.",
          "x-ms-navigationProperty": true
        },
        "@odata.type": {
          "type": "string"
        }
      }
    }
  ],
  "x-ms-discriminator-value": "#microsoft.graph.synchronization"
}
