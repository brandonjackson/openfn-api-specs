{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msgraph/microsoft.graph.scoredEmailAddress",
  "title": "scoredEmailAddress",
  "x-openfn-adaptor": "msgraph",
  "x-source": {
    "openapi": "#/components/schemas/microsoft.graph.scoredEmailAddress",
    "capturedAt": "2026-09-14"
  },
  "required": [
    "@odata.type"
  ],
  "type": "object",
  "properties": {
    "address": {
      "type": [
        "string",
        "null"
      ],
      "description": "The email address."
    },
    "itemId": {
      "type": [
        "string",
        "null"
      ]
    },
    "relevanceScore": {
      "oneOf": [
        {
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        {
          "type": [
            "string",
            "null"
          ]
        },
        {
          "$ref": "ReferenceNumeric.json"
        }
      ],
      "description": "The relevance score of the email address. A relevance score is used as a sort key, in relation to the other returned results. A higher relevance score value corresponds to a more relevant result. Relevance is determined by the user’s communication and collaboration patterns and business relationships."
    },
    "selectionLikelihood": {
      "anyOf": [
        {
          "$ref": "microsoft.graph.selectionLikelihoodInfo.json"
        },
        {
          "type": [
            "object",
            "null"
          ]
        }
      ]
    },
    "@odata.type": {
      "type": "string"
    }
  }
}
