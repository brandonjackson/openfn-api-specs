{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msgraph/microsoft.graph.patternedRecurrence",
  "title": "patternedRecurrence",
  "x-openfn-adaptor": "msgraph",
  "x-source": {
    "openapi": "#/components/schemas/microsoft.graph.patternedRecurrence",
    "capturedAt": "2026-09-14"
  },
  "required": [
    "@odata.type"
  ],
  "type": "object",
  "properties": {
    "pattern": {
      "anyOf": [
        {
          "$ref": "microsoft.graph.recurrencePattern.json"
        },
        {
          "type": [
            "object",
            "null"
          ]
        }
      ],
      "description": "The frequency of an event.  For access reviews: Do not specify this property for a one-time access review.  Only interval, dayOfMonth, and type (weekly, absoluteMonthly) properties of recurrencePattern are supported."
    },
    "range": {
      "anyOf": [
        {
          "$ref": "microsoft.graph.recurrenceRange.json"
        },
        {
          "type": [
            "object",
            "null"
          ]
        }
      ],
      "description": "The duration of an event."
    },
    "@odata.type": {
      "type": "string"
    }
  }
}
