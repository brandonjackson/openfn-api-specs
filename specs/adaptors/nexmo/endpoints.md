# SMS API (nexmo) v1.2.0

<!-- derived from openapi.json by `pnpm specs index nexmo` — do not edit by hand -->

base: https://rest.nexmo.com/sms | auth: not declared | 1 operation in 1 resource

Each line is `METHOD /path — operationId: summary`. For a request body, parameters or response shape, look the operationId up in `openapi.json`; for the objects an operation returns, see `data-schemas/index.json`.

## {format}
- POST /{format} — send-an-sms: Send an SMS
