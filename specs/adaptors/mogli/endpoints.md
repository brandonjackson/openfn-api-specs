# Mogli SMS Apex REST API (generated) (mogli) vv1

<!-- derived from openapi.json by `pnpm specs index mogli` — do not edit by hand -->

base: https://your-instance.my.salesforce.com | auth: not declared | 2 operations in 1 resource

Each line is `METHOD /path — operationId: summary`. For a request body, parameters or response shape, look the operationId up in `openapi.json`; for the objects an operation returns, see `data-schemas/index.json`.

## apexrest
- POST /services/apexrest/Mogli_SMS/v1/sms — createSMS: Create an inbound SMS record
- PUT /services/apexrest/Mogli_SMS/v1/sms — updateSMS: Update an existing SMS record
