{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gemini/CodeExecutionResult",
  "title": "CodeExecutionResult",
  "x-openfn-adaptor": "gemini",
  "x-source": {
    "openapi": "#/components/schemas/CodeExecutionResult",
    "capturedAt": "2026-09-03"
  },
  "type": "object",
  "description": "Result of executing the `ExecutableCode`. Generated only when the `CodeExecution` tool is used.",
  "properties": {
    "output": {
      "type": "string",
      "description": "Optional. Contains stdout when code execution is successful, stderr or other description otherwise."
    },
    "id": {
      "type": "string",
      "description": "Optional. The identifier of the `ExecutableCode` part this result is for. Only populated if the corresponding `ExecutableCode` has an id."
    },
    "outcome": {
      "type": "string",
      "description": "Required. Outcome of the code execution.",
      "enum": [
        "OUTCOME_UNSPECIFIED",
        "OUTCOME_OK",
        "OUTCOME_FAILED",
        "OUTCOME_DEADLINE_EXCEEDED"
      ]
    }
  }
}
