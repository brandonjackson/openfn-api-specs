{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gemini/CountMessageTokensResponse",
  "title": "CountMessageTokensResponse",
  "x-openfn-adaptor": "gemini",
  "x-source": {
    "openapi": "#/components/schemas/CountMessageTokensResponse",
    "capturedAt": "2026-09-03"
  },
  "type": "object",
  "description": "A response from `CountMessageTokens`. It returns the model's `token_count` for the `prompt`.",
  "properties": {
    "tokenCount": {
      "type": "integer",
      "format": "int32",
      "description": "The number of tokens that the `model` tokenizes the `prompt` into. Always non-negative."
    }
  }
}
