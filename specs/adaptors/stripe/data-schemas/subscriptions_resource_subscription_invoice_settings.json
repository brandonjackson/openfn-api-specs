{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/subscriptions_resource_subscription_invoice_settings",
  "title": "SubscriptionsResourceSubscriptionInvoiceSettings",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/subscriptions_resource_subscription_invoice_settings",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "account_tax_ids": {
      "description": "The account tax IDs associated with the subscription. Will be set on invoices generated by the subscription.",
      "items": {
        "anyOf": [
          {
            "maxLength": 5000,
            "type": "string"
          },
          {
            "$ref": "tax_id.json"
          },
          {
            "$ref": "deleted_tax_id.json"
          }
        ],
        "x-expansionResources": {
          "oneOf": [
            {
              "$ref": "tax_id.json"
            },
            {
              "$ref": "deleted_tax_id.json"
            }
          ]
        }
      },
      "type": [
        "array",
        "null"
      ]
    },
    "custom_fields": {
      "description": "A list of up to 4 custom fields to be displayed on the invoice.",
      "items": {
        "$ref": "invoice_setting_custom_field.json"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "description": {
      "description": "An arbitrary string attached to the object. Often useful for displaying to users.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "footer": {
      "description": "Footer to be displayed on the invoice.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "issuer": {
      "$ref": "connect_account_reference.json"
    }
  },
  "required": [
    "issuer"
  ],
  "type": "object",
  "x-expandableFields": [
    "account_tax_ids",
    "custom_fields",
    "issuer"
  ]
}
