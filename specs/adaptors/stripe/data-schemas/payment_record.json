{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/payment_record",
  "title": "PaymentRecord",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/payment_record",
    "capturedAt": "2026-07-06"
  },
  "description": "A Payment Record is a resource that allows you to represent payments that occur on- or off-Stripe.\nFor example, you can create a Payment Record to model a payment made on a different payment processor,\nin order to mark an Invoice as paid and a Subscription as active. Payment Records consist of one or\nmore Payment Attempt Records, which represent individual attempts made on a payment network.",
  "properties": {
    "amount": {
      "$ref": "payments_primitives_payment_records_resource_amount.json"
    },
    "amount_authorized": {
      "$ref": "payments_primitives_payment_records_resource_amount.json"
    },
    "amount_canceled": {
      "$ref": "payments_primitives_payment_records_resource_amount.json"
    },
    "amount_failed": {
      "$ref": "payments_primitives_payment_records_resource_amount.json"
    },
    "amount_guaranteed": {
      "$ref": "payments_primitives_payment_records_resource_amount.json"
    },
    "amount_refunded": {
      "$ref": "payments_primitives_payment_records_resource_amount.json"
    },
    "amount_requested": {
      "$ref": "payments_primitives_payment_records_resource_amount.json"
    },
    "application": {
      "description": "ID of the Connect application that created the PaymentRecord.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "created": {
      "description": "Time at which the object was created. Measured in seconds since the Unix epoch.",
      "format": "unix-time",
      "type": "integer"
    },
    "customer_details": {
      "anyOf": [
        {
          "$ref": "payments_primitives_payment_records_resource_customer_details.json"
        }
      ],
      "description": "Customer information for this payment."
    },
    "customer_presence": {
      "description": "Indicates whether the customer was present in your checkout flow during this payment.",
      "enum": [
        "off_session",
        "on_session"
      ],
      "type": [
        "string",
        "null"
      ]
    },
    "description": {
      "description": "An arbitrary string attached to the object. Often useful for displaying to users.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "id": {
      "description": "Unique identifier for the object.",
      "maxLength": 5000,
      "type": "string"
    },
    "latest_payment_attempt_record": {
      "description": "ID of the latest Payment Attempt Record attached to this Payment Record.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "livemode": {
      "description": "If the object exists in live mode, the value is `true`. If the object exists in test mode, the value is `false`.",
      "type": "boolean"
    },
    "metadata": {
      "additionalProperties": {
        "maxLength": 500,
        "type": "string"
      },
      "description": "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.",
      "type": "object"
    },
    "object": {
      "description": "String representing the object's type. Objects of the same type share the same value.",
      "enum": [
        "payment_record"
      ],
      "type": "string"
    },
    "payment_method_details": {
      "anyOf": [
        {
          "$ref": "payments_primitives_payment_records_resource_payment_method_details.json"
        }
      ],
      "description": "Information about the Payment Method debited for this payment."
    },
    "processor_details": {
      "$ref": "payments_primitives_payment_records_resource_processor_details.json"
    },
    "reported_by": {
      "description": "Indicates who reported the payment.",
      "enum": [
        "self",
        "stripe"
      ],
      "type": "string"
    },
    "shipping_details": {
      "anyOf": [
        {
          "$ref": "payments_primitives_payment_records_resource_shipping_details.json"
        }
      ],
      "description": "Shipping information for this payment."
    }
  },
  "required": [
    "amount",
    "amount_authorized",
    "amount_canceled",
    "amount_failed",
    "amount_guaranteed",
    "amount_refunded",
    "amount_requested",
    "created",
    "id",
    "livemode",
    "metadata",
    "object",
    "processor_details",
    "reported_by"
  ],
  "type": "object",
  "x-expandableFields": [
    "amount",
    "amount_authorized",
    "amount_canceled",
    "amount_failed",
    "amount_guaranteed",
    "amount_refunded",
    "amount_requested",
    "customer_details",
    "payment_method_details",
    "processor_details",
    "shipping_details"
  ],
  "x-resourceId": "payment_record"
}
