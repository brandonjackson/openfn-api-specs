{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/subscription_schedule_add_invoice_item",
  "title": "SubscriptionScheduleAddInvoiceItem",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/subscription_schedule_add_invoice_item",
    "capturedAt": "2026-07-06"
  },
  "description": "An Add Invoice Item describes the prices and quantities that will be added as pending invoice items when entering a phase.",
  "properties": {
    "discountable": {
      "description": "Controls whether discounts apply to this invoice item. Defaults to true if no value is provided.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "discounts": {
      "description": "The stackable discounts that will be applied to the item.",
      "items": {
        "$ref": "discounts_resource_stackable_discount_with_discount_end.json"
      },
      "type": "array"
    },
    "metadata": {
      "additionalProperties": {
        "maxLength": 500,
        "type": "string"
      },
      "description": "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.",
      "type": [
        "object",
        "null"
      ]
    },
    "period": {
      "$ref": "subscription_schedule_add_invoice_item_period.json"
    },
    "price": {
      "anyOf": [
        {
          "maxLength": 5000,
          "type": "string"
        },
        {
          "$ref": "price.json"
        },
        {
          "$ref": "deleted_price.json"
        }
      ],
      "description": "ID of the price used to generate the invoice item.",
      "x-expansionResources": {
        "oneOf": [
          {
            "$ref": "price.json"
          },
          {
            "$ref": "deleted_price.json"
          }
        ]
      }
    },
    "quantity": {
      "description": "The quantity of the invoice item.",
      "type": [
        "integer",
        "null"
      ]
    },
    "tax_rates": {
      "description": "The tax rates which apply to the item. When set, the `default_tax_rates` do not apply to this item.",
      "items": {
        "$ref": "tax_rate.json"
      },
      "type": [
        "array",
        "null"
      ]
    }
  },
  "required": [
    "discounts",
    "period",
    "price"
  ],
  "type": "object",
  "x-expandableFields": [
    "discounts",
    "period",
    "price",
    "tax_rates"
  ]
}
