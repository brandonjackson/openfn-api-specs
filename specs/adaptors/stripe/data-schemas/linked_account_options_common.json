{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/linked_account_options_common",
  "title": "linked_account_options_common",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/linked_account_options_common",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "filters": {
      "$ref": "payment_flows_private_payment_methods_financial_connections_common_linked_account_options_filters.json"
    },
    "permissions": {
      "description": "The list of permissions to request. The `payment_method` permission must be included.",
      "items": {
        "enum": [
          "balances",
          "ownership",
          "payment_method",
          "transactions"
        ],
        "type": "string",
        "x-stripeBypassValidation": true
      },
      "type": "array"
    },
    "prefetch": {
      "description": "Data features requested to be retrieved upon account creation.",
      "items": {
        "enum": [
          "balances",
          "ownership",
          "transactions"
        ],
        "type": "string",
        "x-stripeBypassValidation": true
      },
      "type": [
        "array",
        "null"
      ]
    },
    "return_url": {
      "description": "For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app.",
      "maxLength": 5000,
      "type": "string"
    }
  },
  "type": "object",
  "x-expandableFields": [
    "filters"
  ]
}
