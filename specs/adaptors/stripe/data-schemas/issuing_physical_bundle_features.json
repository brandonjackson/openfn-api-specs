{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/issuing_physical_bundle_features",
  "title": "IssuingPhysicalBundleFeatures",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/issuing_physical_bundle_features",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "card_logo": {
      "description": "The policy for how to use card logo images in a card design with this physical bundle.",
      "enum": [
        "optional",
        "required",
        "unsupported"
      ],
      "type": "string"
    },
    "carrier_text": {
      "description": "The policy for how to use carrier letter text in a card design with this physical bundle.",
      "enum": [
        "optional",
        "required",
        "unsupported"
      ],
      "type": "string"
    },
    "second_line": {
      "description": "The policy for how to use a second line on a card with this physical bundle.",
      "enum": [
        "optional",
        "required",
        "unsupported"
      ],
      "type": "string"
    }
  },
  "required": [
    "card_logo",
    "carrier_text",
    "second_line"
  ],
  "type": "object",
  "x-expandableFields": []
}
