{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/mandate_pix",
  "title": "mandate_pix",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/mandate_pix",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "amount_includes_iof": {
      "description": "Determines if the amount includes the IOF tax.",
      "enum": [
        "always",
        "never"
      ],
      "type": "string"
    },
    "amount_type": {
      "description": "Type of amount.",
      "enum": [
        "fixed",
        "maximum"
      ],
      "type": "string"
    },
    "end_date": {
      "description": "Date when the mandate expires and no further payments will be charged, in `YYYY-MM-DD`.",
      "maxLength": 5000,
      "type": "string"
    },
    "payment_schedule": {
      "description": "Schedule at which the future payments will be charged.",
      "enum": [
        "halfyearly",
        "monthly",
        "quarterly",
        "weekly",
        "yearly"
      ],
      "type": "string"
    },
    "reference": {
      "description": "Subscription name displayed to buyers in their bank app.",
      "maxLength": 5000,
      "type": "string"
    },
    "start_date": {
      "description": "Start date of the mandate, in `YYYY-MM-DD`.",
      "maxLength": 5000,
      "type": "string"
    }
  },
  "type": "object",
  "x-expandableFields": []
}
