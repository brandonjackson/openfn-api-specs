{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/subscriptions_resource_billing_schedules",
  "title": "SubscriptionsResourceBillingSchedules",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/subscriptions_resource_billing_schedules",
    "capturedAt": "2026-07-06"
  },
  "description": "Sets the billing schedule for the subscription.",
  "properties": {
    "applies_to": {
      "description": "Specifies which subscription items the billing schedule applies to.",
      "items": {
        "$ref": "subscriptions_resource_billing_schedules_applies_to.json"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "bill_until": {
      "$ref": "subscriptions_resource_billing_schedules_bill_until.json"
    },
    "key": {
      "description": "Unique identifier for the billing schedule.",
      "maxLength": 5000,
      "type": "string"
    }
  },
  "required": [
    "bill_until",
    "key"
  ],
  "type": "object",
  "x-expandableFields": [
    "applies_to",
    "bill_until"
  ]
}
