{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/issuing_authorization_fleet_cardholder_prompt_data",
  "title": "IssuingAuthorizationFleetCardholderPromptData",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/issuing_authorization_fleet_cardholder_prompt_data",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "alphanumeric_id": {
      "description": "[Deprecated] An alphanumeric ID, though typical point of sales only support numeric entry. The card program can be configured to prompt for a vehicle ID, driver ID, or generic ID.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "driver_id": {
      "description": "Driver ID.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "odometer": {
      "description": "Odometer reading.",
      "type": [
        "integer",
        "null"
      ]
    },
    "unspecified_id": {
      "description": "An alphanumeric ID. This field is used when a vehicle ID, driver ID, or generic ID is entered by the cardholder, but the merchant or card network did not specify the prompt type.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "user_id": {
      "description": "User ID.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "vehicle_number": {
      "description": "Vehicle number.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    }
  },
  "type": "object",
  "x-expandableFields": []
}
