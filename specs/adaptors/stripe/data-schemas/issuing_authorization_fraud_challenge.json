{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/issuing_authorization_fraud_challenge",
  "title": "IssuingAuthorizationFraudChallenge",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/issuing_authorization_fraud_challenge",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "channel": {
      "description": "The method by which the fraud challenge was delivered to the cardholder.",
      "enum": [
        "sms"
      ],
      "type": "string"
    },
    "status": {
      "description": "The status of the fraud challenge.",
      "enum": [
        "expired",
        "pending",
        "rejected",
        "undeliverable",
        "verified"
      ],
      "type": "string"
    },
    "undeliverable_reason": {
      "description": "If the challenge is not deliverable, the reason why.",
      "enum": [
        "no_phone_number",
        "unsupported_phone_number"
      ],
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "channel",
    "status"
  ],
  "type": "object",
  "x-expandableFields": []
}
