{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/subscriptions_resource_billing_schedules_applies_to",
  "title": "SubscriptionsResourceBillingSchedulesAppliesTo",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/subscriptions_resource_billing_schedules_applies_to",
    "capturedAt": "2026-07-06"
  },
  "description": "Represents the entities that the billing schedule applies to.",
  "properties": {
    "price": {
      "anyOf": [
        {
          "maxLength": 5000,
          "type": "string"
        },
        {
          "$ref": "price.json"
        }
      ],
      "description": "The billing schedule will apply to the subscription item with the given price ID.",
      "x-expansionResources": {
        "oneOf": [
          {
            "$ref": "price.json"
          }
        ]
      }
    },
    "type": {
      "description": "Controls which subscription items the billing schedule applies to.",
      "enum": [
        "price"
      ],
      "type": "string"
    }
  },
  "required": [
    "type"
  ],
  "type": "object",
  "x-expandableFields": [
    "price"
  ]
}
