{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/promotion_codes_resource_promotion",
  "title": "PromotionCodesResourcePromotion",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/promotion_codes_resource_promotion",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "coupon": {
      "anyOf": [
        {
          "maxLength": 5000,
          "type": "string"
        },
        {
          "$ref": "coupon.json"
        }
      ],
      "description": "If promotion `type` is `coupon`, the coupon for this promotion.",
      "x-expansionResources": {
        "oneOf": [
          {
            "$ref": "coupon.json"
          }
        ]
      }
    },
    "type": {
      "description": "The type of promotion.",
      "enum": [
        "coupon"
      ],
      "type": "string"
    }
  },
  "required": [
    "type"
  ],
  "type": "object",
  "x-expandableFields": [
    "coupon"
  ]
}
