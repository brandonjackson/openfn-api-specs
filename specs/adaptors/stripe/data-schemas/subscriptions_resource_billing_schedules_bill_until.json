{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/subscriptions_resource_billing_schedules_bill_until",
  "title": "SubscriptionsResourceBillingSchedulesBillUntil",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/subscriptions_resource_billing_schedules_bill_until",
    "capturedAt": "2026-07-06"
  },
  "description": "Specifies the end of billing period.",
  "properties": {
    "computed_timestamp": {
      "description": "The timestamp the billing schedule will apply until.",
      "format": "unix-time",
      "type": "integer"
    },
    "duration": {
      "anyOf": [
        {
          "$ref": "subscriptions_resource_billing_schedules_bill_until_duration.json"
        }
      ],
      "description": "Specifies the billing period."
    },
    "timestamp": {
      "description": "If specified, the billing schedule will apply until the specified timestamp.",
      "format": "unix-time",
      "type": [
        "integer",
        "null"
      ]
    },
    "type": {
      "description": "Describes how the billing schedule will determine the end date. Either `duration` or `timestamp`.",
      "enum": [
        "duration",
        "timestamp"
      ],
      "type": "string"
    }
  },
  "required": [
    "computed_timestamp",
    "type"
  ],
  "type": "object",
  "x-expandableFields": [
    "duration"
  ]
}
