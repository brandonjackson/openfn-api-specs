{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/billing_credit_grants_resource_balance_debit",
  "title": "BillingCreditGrantsResourceBalanceDebit",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/billing_credit_grants_resource_balance_debit",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "amount": {
      "$ref": "billing_credit_grants_resource_amount.json"
    },
    "credits_applied": {
      "anyOf": [
        {
          "$ref": "billing_credit_grants_resource_balance_credits_applied.json"
        }
      ],
      "description": "Details of how the billing credits were applied to an invoice. Only present if `type` is `credits_applied`."
    },
    "type": {
      "description": "The type of debit transaction.",
      "enum": [
        "credits_applied",
        "credits_expired",
        "credits_voided"
      ],
      "type": "string"
    }
  },
  "required": [
    "amount",
    "type"
  ],
  "type": "object",
  "x-expandableFields": [
    "amount",
    "credits_applied"
  ]
}
