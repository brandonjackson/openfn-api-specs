{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/shipping_rate_delivery_estimate",
  "title": "ShippingRateDeliveryEstimate",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/shipping_rate_delivery_estimate",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "maximum": {
      "anyOf": [
        {
          "$ref": "shipping_rate_delivery_estimate_bound.json"
        }
      ],
      "description": "The upper bound of the estimated range. If empty, represents no upper bound i.e., infinite."
    },
    "minimum": {
      "anyOf": [
        {
          "$ref": "shipping_rate_delivery_estimate_bound.json"
        }
      ],
      "description": "The lower bound of the estimated range. If empty, represents no lower bound."
    }
  },
  "type": "object",
  "x-expandableFields": [
    "maximum",
    "minimum"
  ]
}
