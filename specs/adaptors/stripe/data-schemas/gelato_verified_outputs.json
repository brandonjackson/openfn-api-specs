{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/gelato_verified_outputs",
  "title": "GelatoVerifiedOutputs",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/gelato_verified_outputs",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "address": {
      "anyOf": [
        {
          "$ref": "address.json"
        }
      ],
      "description": "The user's verified address."
    },
    "dob": {
      "anyOf": [
        {
          "$ref": "gelato_data_verified_outputs_date.json"
        }
      ],
      "description": "The user’s verified date of birth."
    },
    "email": {
      "description": "The user's verified email address",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "first_name": {
      "description": "The user's verified first name.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "id_number": {
      "description": "The user's verified id number.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "id_number_type": {
      "description": "The user's verified id number type.",
      "enum": [
        "br_cpf",
        "sg_nric",
        "us_ssn"
      ],
      "type": [
        "string",
        "null"
      ]
    },
    "last_name": {
      "description": "The user's verified last name.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "phone": {
      "description": "The user's verified phone number",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "sex": {
      "description": "The user's verified sex.",
      "enum": [
        "[redacted]",
        "female",
        "male",
        "unknown"
      ],
      "type": [
        "string",
        "null"
      ]
    },
    "unparsed_place_of_birth": {
      "description": "The user's verified place of birth as it appears in the document.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "unparsed_sex": {
      "description": "The user's verified sex as it appears in the document.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    }
  },
  "type": "object",
  "x-expandableFields": [
    "address",
    "dob"
  ]
}
