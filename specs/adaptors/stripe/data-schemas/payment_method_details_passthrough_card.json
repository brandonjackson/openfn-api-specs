{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/payment_method_details_passthrough_card",
  "title": "payment_method_details_passthrough_card",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/payment_method_details_passthrough_card",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "brand": {
      "description": "Card brand. Can be `amex`, `cartes_bancaires`, `diners`, `discover`, `eftpos_au`, `jcb`, `link`, `mastercard`, `unionpay`, `visa` or `unknown`.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "country": {
      "description": "Two-letter ISO code representing the country of the card. You could use this attribute to get a sense of the international breakdown of cards you've collected.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "exp_month": {
      "description": "Two-digit number representing the card's expiration month.",
      "type": [
        "integer",
        "null"
      ]
    },
    "exp_year": {
      "description": "Four-digit number representing the card's expiration year.",
      "type": [
        "integer",
        "null"
      ]
    },
    "funding": {
      "description": "Card funding type. Can be `credit`, `debit`, `prepaid`, or `unknown`.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "last4": {
      "description": "The last four digits of the card.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    }
  },
  "type": "object",
  "x-expandableFields": []
}
