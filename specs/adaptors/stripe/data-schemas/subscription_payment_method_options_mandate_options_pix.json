{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/subscription_payment_method_options_mandate_options_pix",
  "title": "subscription_payment_method_options_mandate_options_pix",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/subscription_payment_method_options_mandate_options_pix",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "amount": {
      "description": "Amount to be charged for future payments.",
      "type": [
        "integer",
        "null"
      ]
    },
    "amount_includes_iof": {
      "description": "Determines if the amount includes the IOF tax.",
      "enum": [
        "always",
        "never"
      ],
      "type": [
        "string",
        "null"
      ]
    },
    "end_date": {
      "description": "Date when the mandate expires and no further payments will be charged, in `YYYY-MM-DD`.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "payment_schedule": {
      "description": "Schedule at which the future payments will be charged.",
      "enum": [
        "halfyearly",
        "monthly",
        "quarterly",
        "weekly",
        "yearly"
      ],
      "type": [
        "string",
        "null"
      ]
    }
  },
  "type": "object",
  "x-expandableFields": []
}
