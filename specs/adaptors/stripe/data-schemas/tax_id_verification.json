{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/tax_id_verification",
  "title": "tax_id_verification",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/tax_id_verification",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "status": {
      "description": "Verification status, one of `pending`, `verified`, `unverified`, or `unavailable`.",
      "enum": [
        "pending",
        "unavailable",
        "unverified",
        "verified"
      ],
      "type": "string"
    },
    "verified_address": {
      "description": "Verified address.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "verified_name": {
      "description": "Verified name.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "status"
  ],
  "type": "object",
  "x-expandableFields": []
}
