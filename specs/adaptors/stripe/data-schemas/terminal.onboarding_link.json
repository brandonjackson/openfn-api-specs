{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/terminal.onboarding_link",
  "title": "TerminalOnboardingLinkOnboardingLink",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/terminal.onboarding_link",
    "capturedAt": "2026-07-06"
  },
  "description": "Returns redirect links used for onboarding onto Tap to Pay on iPhone.",
  "properties": {
    "link_options": {
      "$ref": "terminal_onboarding_link_link_options.json"
    },
    "link_type": {
      "description": "The type of link being generated.",
      "enum": [
        "apple_terms_and_conditions"
      ],
      "type": "string"
    },
    "object": {
      "enum": [
        "terminal.onboarding_link"
      ],
      "type": "string"
    },
    "on_behalf_of": {
      "description": "Stripe account ID to generate the link for.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "redirect_url": {
      "description": "The link passed back to the user for their onboarding.",
      "maxLength": 5000,
      "type": "string"
    }
  },
  "required": [
    "link_options",
    "link_type",
    "object",
    "redirect_url"
  ],
  "type": "object",
  "x-expandableFields": [
    "link_options"
  ],
  "x-resourceId": "terminal.onboarding_link"
}
