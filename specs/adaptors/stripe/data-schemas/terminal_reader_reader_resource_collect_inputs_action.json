{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/terminal_reader_reader_resource_collect_inputs_action",
  "title": "TerminalReaderReaderResourceCollectInputsAction",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/terminal_reader_reader_resource_collect_inputs_action",
    "capturedAt": "2026-07-06"
  },
  "description": "Represents a reader action to collect customer inputs",
  "properties": {
    "inputs": {
      "description": "List of inputs to be collected.",
      "items": {
        "$ref": "terminal_reader_reader_resource_input.json"
      },
      "type": "array"
    },
    "metadata": {
      "additionalProperties": {
        "maxLength": 500,
        "type": "string"
      },
      "description": "Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.",
      "type": [
        "object",
        "null"
      ]
    }
  },
  "required": [
    "inputs"
  ],
  "type": "object",
  "x-expandableFields": [
    "inputs"
  ]
}
