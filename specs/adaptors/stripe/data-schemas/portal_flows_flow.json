{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/portal_flows_flow",
  "title": "PortalFlowsFlow",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/portal_flows_flow",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "after_completion": {
      "$ref": "portal_flows_flow_after_completion.json"
    },
    "subscription_cancel": {
      "anyOf": [
        {
          "$ref": "portal_flows_flow_subscription_cancel.json"
        }
      ],
      "description": "Configuration when `flow.type=subscription_cancel`."
    },
    "subscription_update": {
      "anyOf": [
        {
          "$ref": "portal_flows_flow_subscription_update.json"
        }
      ],
      "description": "Configuration when `flow.type=subscription_update`."
    },
    "subscription_update_confirm": {
      "anyOf": [
        {
          "$ref": "portal_flows_flow_subscription_update_confirm.json"
        }
      ],
      "description": "Configuration when `flow.type=subscription_update_confirm`."
    },
    "type": {
      "description": "Type of flow that the customer will go through.",
      "enum": [
        "payment_method_update",
        "subscription_cancel",
        "subscription_update",
        "subscription_update_confirm"
      ],
      "type": "string"
    }
  },
  "required": [
    "after_completion",
    "type"
  ],
  "type": "object",
  "x-expandableFields": [
    "after_completion",
    "subscription_cancel",
    "subscription_update",
    "subscription_update_confirm"
  ]
}
