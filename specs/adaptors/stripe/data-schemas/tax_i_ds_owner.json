{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/tax_i_ds_owner",
  "title": "TaxIDsOwner",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/tax_i_ds_owner",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "account": {
      "anyOf": [
        {
          "maxLength": 5000,
          "type": "string"
        },
        {
          "$ref": "account.json"
        }
      ],
      "description": "The account being referenced when `type` is `account`.",
      "x-expansionResources": {
        "oneOf": [
          {
            "$ref": "account.json"
          }
        ]
      }
    },
    "application": {
      "anyOf": [
        {
          "maxLength": 5000,
          "type": "string"
        },
        {
          "$ref": "application.json"
        }
      ],
      "description": "The Connect Application being referenced when `type` is `application`.",
      "x-expansionResources": {
        "oneOf": [
          {
            "$ref": "application.json"
          }
        ]
      }
    },
    "customer": {
      "anyOf": [
        {
          "maxLength": 5000,
          "type": "string"
        },
        {
          "$ref": "customer.json"
        }
      ],
      "description": "The customer being referenced when `type` is `customer`.",
      "x-expansionResources": {
        "oneOf": [
          {
            "$ref": "customer.json"
          }
        ]
      }
    },
    "customer_account": {
      "description": "The Account representing the customer being referenced when `type` is `customer`.",
      "maxLength": 5000,
      "type": [
        "string",
        "null"
      ]
    },
    "type": {
      "description": "Type of owner referenced.",
      "enum": [
        "account",
        "application",
        "customer",
        "self"
      ],
      "type": "string"
    }
  },
  "required": [
    "type"
  ],
  "type": "object",
  "x-expandableFields": [
    "account",
    "application",
    "customer"
  ]
}
