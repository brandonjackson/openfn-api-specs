{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/portal_flows_flow_after_completion",
  "title": "PortalFlowsFlowAfterCompletion",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/portal_flows_flow_after_completion",
    "capturedAt": "2026-07-06"
  },
  "description": "",
  "properties": {
    "hosted_confirmation": {
      "anyOf": [
        {
          "$ref": "portal_flows_after_completion_hosted_confirmation.json"
        }
      ],
      "description": "Configuration when `after_completion.type=hosted_confirmation`."
    },
    "redirect": {
      "anyOf": [
        {
          "$ref": "portal_flows_after_completion_redirect.json"
        }
      ],
      "description": "Configuration when `after_completion.type=redirect`."
    },
    "type": {
      "description": "The specified type of behavior after the flow is completed.",
      "enum": [
        "hosted_confirmation",
        "portal_homepage",
        "redirect"
      ],
      "type": "string"
    }
  },
  "required": [
    "type"
  ],
  "type": "object",
  "x-expandableFields": [
    "hosted_confirmation",
    "redirect"
  ]
}
