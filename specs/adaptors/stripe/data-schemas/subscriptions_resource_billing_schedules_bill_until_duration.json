{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stripe/subscriptions_resource_billing_schedules_bill_until_duration",
  "title": "SubscriptionsResourceBillingSchedulesBillUntilDuration",
  "x-openfn-adaptor": "stripe",
  "x-source": {
    "openapi": "#/components/schemas/subscriptions_resource_billing_schedules_bill_until_duration",
    "capturedAt": "2026-07-06"
  },
  "description": "Configures the `bill_until` date based on the provided `interval` and `interval_count`.",
  "properties": {
    "interval": {
      "description": "Specifies billing duration. Either `day`, `week`, `month` or `year`.",
      "enum": [
        "day",
        "month",
        "week",
        "year"
      ],
      "type": "string"
    },
    "interval_count": {
      "description": "The multiplier applied to the interval.",
      "type": [
        "integer",
        "null"
      ]
    }
  },
  "required": [
    "interval"
  ],
  "type": "object",
  "x-expandableFields": []
}
