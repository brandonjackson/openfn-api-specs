{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "et-mfr/FacilitySummaryList",
  "title": "FacilitySummaryList",
  "x-openfn-adaptor": "et-mfr",
  "x-source": {
    "openapi": "#/components/schemas/FacilitySummaryList",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "description": "One page of facility summaries, in the standard envelope.",
  "properties": {
    "result": {
      "type": "integer",
      "description": "Status code of the call itself: 0 on success, 1 when the API rejects the request (for example an unknown lookup type). Independent of the HTTP status."
    },
    "message": {
      "type": "string",
      "description": "Human-readable outcome message."
    },
    "model": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "FacilitySummary.json"
      }
    }
  }
}
