{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "et-mfr/ApiResponse",
  "title": "ApiResponse",
  "x-openfn-adaptor": "et-mfr",
  "x-source": {
    "openapi": "#/components/schemas/ApiResponse",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "description": "The envelope every MFR API operation answers with. The adaptor unwraps `model` onto state.data.",
  "properties": {
    "result": {
      "type": "integer",
      "description": "Status code of the call itself: 0 on success, 1 when the API rejects the request (for example an unknown lookup type). Independent of the HTTP status."
    },
    "message": {
      "type": "string",
      "description": "Human-readable outcome message."
    },
    "model": {
      "description": "The payload, when the operation returns one."
    }
  }
}
