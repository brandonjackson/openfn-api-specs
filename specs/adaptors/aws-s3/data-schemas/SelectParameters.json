{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "aws-s3/SelectParameters",
  "title": "SelectParameters",
  "x-openfn-adaptor": "aws-s3",
  "x-source": {
    "openapi": "#/components/schemas/SelectParameters",
    "capturedAt": "2026-09-03"
  },
  "type": "object",
  "required": [
    "InputSerialization",
    "ExpressionType",
    "Expression",
    "OutputSerialization"
  ],
  "properties": {
    "InputSerialization": {
      "allOf": [
        {
          "$ref": "InputSerialization.json"
        },
        {
          "description": "Describes the serialization format of the object."
        }
      ]
    },
    "ExpressionType": {
      "allOf": [
        {
          "$ref": "ExpressionType.json"
        },
        {
          "description": "The type of the provided expression (for example, SQL)."
        }
      ]
    },
    "Expression": {
      "allOf": [
        {
          "$ref": "Expression.json"
        },
        {
          "description": "The expression that is used to query the object."
        }
      ]
    },
    "OutputSerialization": {
      "allOf": [
        {
          "$ref": "OutputSerialization.json"
        },
        {
          "description": "Describes how the results of the Select job are serialized."
        }
      ]
    }
  },
  "description": "Describes the parameters for Select job types."
}
