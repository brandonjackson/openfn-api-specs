{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "aws-s3/InventoryDestination",
  "title": "InventoryDestination",
  "x-openfn-adaptor": "aws-s3",
  "x-source": {
    "openapi": "#/components/schemas/InventoryDestination",
    "capturedAt": "2026-09-03"
  },
  "type": "object",
  "required": [
    "S3BucketDestination"
  ],
  "properties": {
    "S3BucketDestination": {
      "allOf": [
        {
          "$ref": "InventoryS3BucketDestination.json"
        },
        {
          "description": "Contains the bucket name, file format, bucket owner (optional), and prefix (optional) where inventory results are published."
        }
      ]
    }
  },
  "description": "Specifies the inventory configuration for an Amazon S3 bucket."
}
