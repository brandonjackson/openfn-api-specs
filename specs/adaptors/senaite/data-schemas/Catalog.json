{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "senaite/Catalog",
  "title": "Catalog",
  "x-openfn-adaptor": "senaite",
  "x-source": {
    "openapi": "#/components/schemas/Catalog",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "description": "A catalog registered in the SENAITE instance, with the indexes that can be used as search filters or sort keys, the metadata schema it stores, and the portal types it indexes.",
  "additionalProperties": true,
  "properties": {
    "id": {
      "type": "string",
      "description": "Catalog id, e.g. `senaite_catalog_sample`."
    },
    "indexes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "schema": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Metadata columns the catalog stores."
    },
    "portal_types": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "_runtime": {
      "type": "number"
    }
  }
}
