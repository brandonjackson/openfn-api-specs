{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "asana/PortfolioMembershipCompact",
  "title": "PortfolioMembershipCompact",
  "x-openfn-adaptor": "asana",
  "x-source": {
    "openapi": "#/components/schemas/PortfolioMembershipCompact",
    "capturedAt": "2026-07-06"
  },
  "description": "This object determines if a user is a member of a portfolio.",
  "type": "object",
  "properties": {
    "gid": {
      "description": "Globally unique identifier of the resource, as a string.",
      "type": "string",
      "readOnly": true,
      "example": "12345",
      "x-insert-after": false
    },
    "resource_type": {
      "description": "The base type of this resource.",
      "type": "string",
      "readOnly": true,
      "example": "portfolio_membership",
      "x-insert-after": "gid"
    },
    "parent": {
      "description": "The portfolio the user is a member of.",
      "$ref": "PortfolioCompact.json"
    },
    "member": {
      "description": "Member can be a user or a team.",
      "$ref": "MemberCompact.json"
    },
    "access_level": {
      "description": "Whether the member has admin, editor, or viewer access to the portfolio. Portfolios do not support commenter access yet.",
      "type": "string",
      "enum": [
        "admin",
        "editor",
        "viewer"
      ],
      "readOnly": true,
      "example": "admin"
    }
  }
}
