{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ihris/LoginResponse",
  "title": "LoginResponse",
  "x-openfn-adaptor": "ihris",
  "x-source": {
    "openapi": "#/components/schemas/LoginResponse",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "description": "Result of POST /auth/login. The session itself is carried by the `Set-Cookie` response header, which is what the OpenFn adaptor stores and replays.",
  "properties": {
    "ok": {
      "type": "boolean"
    },
    "user": {
      "type": "object",
      "additionalProperties": true,
      "description": "The signed-in user, as an iHRIS user object wrapping its FHIR Person/Practitioner `resource` (password and salt extensions stripped)."
    }
  },
  "additionalProperties": true
}
