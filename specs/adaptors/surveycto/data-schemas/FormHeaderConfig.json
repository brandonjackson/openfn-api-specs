{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "surveycto/FormHeaderConfig",
  "title": "FormHeaderConfig",
  "x-openfn-adaptor": "surveycto",
  "x-source": {
    "openapi": "#/components/schemas/FormHeaderConfig",
    "capturedAt": "2026-09-14"
  },
  "type": "object",
  "properties": {
    "save_form_label": {
      "type": "string",
      "description": "Custom label for the \"Save\" button. Null = system default.",
      "example": "Save Progress"
    },
    "pinned_forms_icon": {
      "type": "string",
      "description": "Custom FontAwesome icon for the pinned forms entry. Must be a plain FontAwesome class name matching fa-<name>.",
      "example": "fa-list"
    },
    "pinned_forms_label": {
      "type": "string",
      "description": "Custom label for the \"Forms\" (pinned forms) menu entry and modal title. Null = system default.",
      "example": "Quick Forms"
    },
    "show_options_button": {
      "type": "boolean",
      "description": "Show the \"Options\" dropdown menu. Default: true.",
      "example": "true"
    },
    "home_label": {
      "type": "string",
      "description": "Custom label for the \"Home\" breadcrumb (next to the logo) and the drawer's \"Main page\" link. Null = system default.",
      "example": "Dashboard"
    },
    "show_save_form_button": {
      "type": "boolean",
      "description": "Show the \"Save Form\" button in the drawer menu and navbar. Default: true.",
      "example": "true"
    },
    "show_home_link": {
      "type": "boolean",
      "description": "Show the \"Home\" breadcrumb (next to the logo) and the form screen's \"Main page\" drawer entry. When false, only those two links are hidden -- the Exit/Return links and the post-exit redirects still navigate back to the main menu. Only meaningful while a profile is attached. Default: true.",
      "example": "true"
    },
    "show_languages": {
      "type": "boolean",
      "description": "Show the language selector. Default: true.",
      "example": "true"
    },
    "custom_items": {
      "type": "array",
      "description": "Custom navigation items shown in the form-filling drawer and navbar alongside built-in items (before the pinned-forms entry). Same structure as menu_config.custom_items.",
      "items": {
        "$ref": "CustomMenuItem.json"
      }
    },
    "main_page_url": {
      "type": "string",
      "description": "Target of the \"Home\" / \"Main page\" link in the header. A relative path starting with '/' or an absolute https URL. Null = Collect main menu.",
      "example": "/collect/cases.html"
    }
  },
  "description": "Form-filling header/navbar configuration: button visibility, label overrides, and custom navigation items. Form-filling behavior settings (auto-send, skip welcome, etc.) are in form_filling_config. All fields are optional; omitted fields keep their defaults."
}
