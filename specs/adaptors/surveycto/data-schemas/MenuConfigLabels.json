{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "surveycto/MenuConfigLabels",
  "title": "MenuConfigLabels",
  "x-openfn-adaptor": "surveycto",
  "x-source": {
    "openapi": "#/components/schemas/MenuConfigLabels",
    "capturedAt": "2026-09-14"
  },
  "type": "object",
  "properties": {
    "delete_saved_form_label": {
      "type": "string",
      "description": "Translated label for the \"Delete Saved Form\" button. Null = fall through to the base config.",
      "example": "Supprimer un formulaire"
    },
    "manage_cases_label": {
      "type": "string",
      "description": "Translated label for the \"Manage Cases\" button. Null = fall through to the base config.",
      "example": "Mes cas"
    },
    "fill_blank_form_label": {
      "type": "string",
      "description": "Translated label for the \"Fill Blank Form\" button. Null = fall through to the base config.",
      "example": "Remplir un formulaire"
    },
    "edit_saved_form_label": {
      "type": "string",
      "description": "Translated label for the \"Edit Saved Form\" button. Null = fall through to the base config.",
      "example": "Modifier un formulaire"
    },
    "custom_items": {
      "type": "array",
      "description": "Per-item label translations, matched by the stable id of the corresponding item in the base menu_config.custom_items.",
      "items": {
        "$ref": "CustomItemLabelOverride.json"
      }
    }
  },
  "description": "Label-only subset of menu_config used inside locale_overrides: translated menu button labels and custom item labels. All fields are optional; omitted fields keep their defaults."
}
