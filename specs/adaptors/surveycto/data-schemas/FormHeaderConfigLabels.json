{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "surveycto/FormHeaderConfigLabels",
  "title": "FormHeaderConfigLabels",
  "x-openfn-adaptor": "surveycto",
  "x-source": {
    "openapi": "#/components/schemas/FormHeaderConfigLabels",
    "capturedAt": "2026-09-14"
  },
  "type": "object",
  "properties": {
    "save_form_label": {
      "type": "string",
      "description": "Translated label for the \"Save\" button. Null = fall through to the base config.",
      "example": "Enregistrer"
    },
    "pinned_forms_label": {
      "type": "string",
      "description": "Translated label for the \"Forms\" (pinned forms) menu entry. Null = fall through to the base config.",
      "example": "Formulaires rapides"
    },
    "home_label": {
      "type": "string",
      "description": "Translated label for the \"Home\" / \"Main page\" link. Null = fall through to the base config.",
      "example": "Tableau de bord"
    },
    "custom_items": {
      "type": "array",
      "description": "Per-item label translations, matched by the stable id of the corresponding item in the base form_header_config.custom_items.",
      "items": {
        "$ref": "CustomItemLabelOverride.json"
      }
    }
  },
  "description": "Label-only subset of form_header_config used inside locale_overrides: translated header/drawer labels and custom item labels. All fields are optional; omitted fields keep their defaults."
}
