{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "surveycto/MenuConfig",
  "title": "MenuConfig",
  "x-openfn-adaptor": "surveycto",
  "x-source": {
    "openapi": "#/components/schemas/MenuConfig",
    "capturedAt": "2026-09-14"
  },
  "type": "object",
  "properties": {
    "layout": {
      "type": "string",
      "description": "Layout mode for the main menu page: \"default\" (vertical buttons), \"header_bar\" (horizontal links in the top navbar; hides the colored header bar), or \"custom\" (replace default items with custom_items). Null = \"default\".",
      "example": "default",
      "enum": [
        "default",
        "header_bar",
        "custom"
      ]
    },
    "show_fill_blank_form": {
      "type": "boolean",
      "description": "Show the \"Fill Blank Form\" button. Default: true.",
      "example": "true"
    },
    "show_edit_saved_form": {
      "type": "boolean",
      "description": "Show the \"Edit Saved Form\" button. Default: true.",
      "example": "true"
    },
    "show_manage_cases": {
      "type": "boolean",
      "description": "Show the \"Manage Cases\" button (only shown if cases are enabled on the server). Default: true.",
      "example": "true"
    },
    "delete_saved_form_label": {
      "type": "string",
      "description": "Custom label for the \"Delete Saved Form\" button. Null = system default (i18n).",
      "example": "Discard Survey"
    },
    "manage_cases_label": {
      "type": "string",
      "description": "Custom label for the \"Manage Cases\" button. Null = system default (i18n).",
      "example": "My Cases"
    },
    "fill_blank_form_label": {
      "type": "string",
      "description": "Custom label for the \"Fill Blank Form\" button. Null = system default (i18n).",
      "example": "Start New Survey"
    },
    "show_delete_saved_form": {
      "type": "boolean",
      "description": "Show the \"Delete Saved Form\" button. Default: true.",
      "example": "true"
    },
    "edit_saved_form_label": {
      "type": "string",
      "description": "Custom label for the \"Edit Saved Form\" button. Null = system default (i18n).",
      "example": "Continue Survey"
    },
    "custom_items": {
      "type": "array",
      "description": "Custom menu items. In \"custom\" layout they replace the default items; in \"header_bar\" layout they are appended after the defaults.",
      "items": {
        "$ref": "CustomMenuItem.json"
      }
    }
  },
  "description": "Main menu configuration: which buttons appear on the Collect main menu page, their layout, custom labels, and optional custom items."
}
