{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "surveycto/CustomItemLabelOverride",
  "title": "CustomItemLabelOverride",
  "x-openfn-adaptor": "surveycto",
  "x-source": {
    "openapi": "#/components/schemas/CustomItemLabelOverride",
    "capturedAt": "2026-09-14"
  },
  "required": [
    "id"
  ],
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The id of the custom item in the base config this translation applies to. Must match an existing item id.",
      "example": "my_cases"
    },
    "label": {
      "type": "string",
      "description": "The translated display text for the item.",
      "example": "Mes cas"
    }
  },
  "description": "Translated label for one custom item, referenced by the item's stable id in the base config's custom_items list. Only the label can be translated; the item structure stays the same."
}
