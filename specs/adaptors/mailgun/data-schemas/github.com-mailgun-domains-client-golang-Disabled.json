{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "mailgun/github.com-mailgun-domains-client-golang-Disabled",
  "title": "github.com-mailgun-domains-client-golang-Disabled",
  "x-openfn-adaptor": "mailgun",
  "x-source": {
    "openapi": "#/components/schemas/github.com-mailgun-domains-client-golang-Disabled",
    "capturedAt": "2026-07-06"
  },
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "description": "Code indicating the reason for the domain being disabled"
    },
    "note": {
      "type": "string",
      "description": "Additional information about why the domain was disabled"
    },
    "permanently": {
      "type": "boolean",
      "description": "Indicates whether the domain is permanently disabled"
    },
    "reason": {
      "type": "string",
      "description": "Additional information about why the domain was disabled"
    },
    "until": {
      "type": "string",
      "description": "Timestamp in RFC1123 format indicating when the domain will be re-enabled if the disablement is temporary"
    }
  },
  "required": [
    "code",
    "note",
    "permanently",
    "reason"
  ]
}
