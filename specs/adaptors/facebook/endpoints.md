# Facebook Messenger Send API (subset) (facebook) v2.6

<!-- derived from openapi.json by `pnpm specs index facebook` — do not edit by hand -->

base: https://graph.facebook.com/v2.6 | auth: not declared | 1 operation in 1 resource

Each line is `METHOD /path — operationId: summary`. For a request body, parameters or response shape, look the operationId up in `openapi.json`; for the objects an operation returns, see `data-schemas/index.json`.

## me
- POST /me/messages — postMessage: Send a message via the Messenger Send API
