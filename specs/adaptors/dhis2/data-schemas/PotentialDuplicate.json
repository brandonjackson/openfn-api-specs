{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "dhis2/PotentialDuplicate",
  "title": "PotentialDuplicate",
  "x-openfn-adaptor": "dhis2",
  "x-source": {
    "openapi": "#/components/schemas/PotentialDuplicate",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "required": [
    "status"
  ],
  "properties": {
    "created": {
      "type": "string",
      "format": "date-time"
    },
    "createdByUserName": {
      "type": "string"
    },
    "duplicate": {
      "type": "string",
      "format": "uid",
      "minLength": 11,
      "maxLength": 11,
      "pattern": "^[a-zA-Z][a-zA-Z0-9]{10}$"
    },
    "lastUpdated": {
      "type": "string",
      "format": "date-time"
    },
    "lastUpdatedByUserName": {
      "type": "string"
    },
    "original": {
      "type": "string",
      "format": "uid",
      "minLength": 11,
      "maxLength": 11,
      "pattern": "^[a-zA-Z][a-zA-Z0-9]{10}$"
    },
    "status": {
      "$ref": "DeduplicationStatus.json"
    }
  }
}
