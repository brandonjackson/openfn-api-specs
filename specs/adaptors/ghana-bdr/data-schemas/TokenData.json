{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ghana-bdr/TokenData",
  "title": "TokenData",
  "x-openfn-adaptor": "ghana-bdr",
  "x-source": {
    "openapi": "#/components/schemas/TokenData",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "description": "An issued token pair.",
  "properties": {
    "access_token": {
      "type": "string",
      "example": "REPLACE_WITH_ACCESS_TOKEN",
      "description": "Short-lived access token; send it as the Token header (the OpenFn adaptor sends it as Authorization: Bearer instead)."
    },
    "refresh_token": {
      "type": "string",
      "example": "REPLACE_WITH_REFRESH_TOKEN",
      "description": "Rotating refresh token; trade it at the refresh endpoint for a new pair."
    },
    "token_type": {
      "type": "string",
      "example": "Bearer"
    },
    "expires_in": {
      "type": "integer",
      "example": 3600,
      "description": "Lifetime of access_token in seconds (60 minutes)."
    }
  }
}
