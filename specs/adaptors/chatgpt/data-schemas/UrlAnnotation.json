{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "chatgpt/UrlAnnotation",
  "title": "URL annotation",
  "x-openfn-adaptor": "chatgpt",
  "x-source": {
    "openapi": "#/components/schemas/UrlAnnotation",
    "capturedAt": "2026-09-03"
  },
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "url"
      ],
      "description": "Type discriminator that is always `url` for this annotation.",
      "default": "url",
      "x-stainless-const": true
    },
    "source": {
      "$ref": "UrlAnnotationSource.json",
      "description": "URL referenced by the annotation."
    }
  },
  "type": "object",
  "required": [
    "type",
    "source"
  ],
  "description": "Annotation that references a URL."
}
