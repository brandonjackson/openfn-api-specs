{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "chatgpt/BetaApplyPatchToolParam",
  "title": "Apply patch tool",
  "x-openfn-adaptor": "chatgpt",
  "x-source": {
    "openapi": "#/components/schemas/BetaApplyPatchToolParam",
    "capturedAt": "2026-09-03"
  },
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "apply_patch"
      ],
      "description": "The type of the tool. Always `apply_patch`.",
      "default": "apply_patch",
      "x-stainless-const": true
    },
    "allowed_callers": {
      "anyOf": [
        {
          "items": {
            "$ref": "BetaCallableToolAllowedCaller.json"
          },
          "type": "array",
          "minItems": 1,
          "description": "The tool invocation context(s)."
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "type": "object",
  "required": [
    "type"
  ],
  "description": "Allows the assistant to create, delete, or update files using unified diffs."
}
