{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "chatgpt/ProjectApiKeyOwnerUser",
  "title": "ProjectApiKeyOwnerUser",
  "x-openfn-adaptor": "chatgpt",
  "x-source": {
    "openapi": "#/components/schemas/ProjectApiKeyOwnerUser",
    "capturedAt": "2026-09-03"
  },
  "type": "object",
  "description": "The user that owns a project API key.",
  "properties": {
    "id": {
      "type": "string",
      "description": "The identifier, which can be referenced in API endpoints"
    },
    "email": {
      "type": "string",
      "description": "The email address of the user."
    },
    "name": {
      "type": "string",
      "description": "The name of the user."
    },
    "created_at": {
      "type": "integer",
      "format": "unixtime",
      "description": "The Unix timestamp (in seconds) of when the user was created."
    },
    "role": {
      "type": "string",
      "description": "The user's project role."
    }
  },
  "required": [
    "id",
    "email",
    "name",
    "created_at",
    "role"
  ]
}
