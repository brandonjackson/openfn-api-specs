{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "chatgpt/FileAnnotation",
  "title": "File annotation",
  "x-openfn-adaptor": "chatgpt",
  "x-source": {
    "openapi": "#/components/schemas/FileAnnotation",
    "capturedAt": "2026-09-03"
  },
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "file"
      ],
      "description": "Type discriminator that is always `file` for this annotation.",
      "default": "file",
      "x-stainless-const": true
    },
    "source": {
      "$ref": "FileAnnotationSource.json",
      "description": "File attachment referenced by the annotation."
    }
  },
  "type": "object",
  "required": [
    "type",
    "source"
  ],
  "description": "Annotation that references an uploaded file."
}
