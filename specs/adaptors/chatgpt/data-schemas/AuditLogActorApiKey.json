{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "chatgpt/AuditLogActorApiKey",
  "title": "AuditLogActorApiKey",
  "x-openfn-adaptor": "chatgpt",
  "x-source": {
    "openapi": "#/components/schemas/AuditLogActorApiKey",
    "capturedAt": "2026-09-03"
  },
  "type": "object",
  "description": "The API Key used to perform the audit logged action.",
  "properties": {
    "id": {
      "type": "string",
      "description": "The tracking id of the API key."
    },
    "type": {
      "type": "string",
      "description": "The type of API key. Can be either `user` or `service_account`.",
      "enum": [
        "user",
        "service_account"
      ]
    },
    "user": {
      "$ref": "AuditLogActorUser.json"
    },
    "service_account": {
      "$ref": "AuditLogActorServiceAccount.json"
    }
  }
}
