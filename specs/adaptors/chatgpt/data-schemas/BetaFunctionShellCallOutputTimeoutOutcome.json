{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "chatgpt/BetaFunctionShellCallOutputTimeoutOutcome",
  "title": "Shell call timeout outcome",
  "x-openfn-adaptor": "chatgpt",
  "x-source": {
    "openapi": "#/components/schemas/BetaFunctionShellCallOutputTimeoutOutcome",
    "capturedAt": "2026-09-03"
  },
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "timeout"
      ],
      "description": "The outcome type. Always `timeout`.",
      "default": "timeout",
      "x-stainless-const": true
    }
  },
  "type": "object",
  "required": [
    "type"
  ],
  "description": "Indicates that the shell call exceeded its configured time limit."
}
