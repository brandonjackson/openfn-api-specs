{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "googlesheets/ChartData",
  "title": "ChartData",
  "x-openfn-adaptor": "googlesheets",
  "x-source": {
    "openapi": "#/components/schemas/ChartData",
    "capturedAt": "2026-07-06"
  },
  "type": "object",
  "description": "The data included in a domain or series.",
  "properties": {
    "groupRule": {
      "$ref": "ChartGroupRule.json"
    },
    "sourceRange": {
      "$ref": "ChartSourceRange.json"
    },
    "columnReference": {
      "$ref": "DataSourceColumnReference.json"
    },
    "aggregateType": {
      "type": "string",
      "description": "The aggregation type for the series of a data source chart. Only supported for data source charts.",
      "enum": [
        "CHART_AGGREGATE_TYPE_UNSPECIFIED",
        "AVERAGE",
        "COUNT",
        "MAX",
        "MEDIAN",
        "MIN",
        "SUM"
      ]
    }
  }
}
