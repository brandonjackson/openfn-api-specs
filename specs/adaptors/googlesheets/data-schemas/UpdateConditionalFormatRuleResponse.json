{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "googlesheets/UpdateConditionalFormatRuleResponse",
  "title": "UpdateConditionalFormatRuleResponse",
  "x-openfn-adaptor": "googlesheets",
  "x-source": {
    "openapi": "#/components/schemas/UpdateConditionalFormatRuleResponse",
    "capturedAt": "2026-07-06"
  },
  "type": "object",
  "description": "The result of updating a conditional format rule.",
  "properties": {
    "oldRule": {
      "$ref": "ConditionalFormatRule.json"
    },
    "newIndex": {
      "type": "integer",
      "format": "int32",
      "description": "The index of the new rule."
    },
    "oldIndex": {
      "type": "integer",
      "format": "int32",
      "description": "The old index of the rule. Not set if a rule was replaced (because it is the same as new_index)."
    },
    "newRule": {
      "$ref": "ConditionalFormatRule.json"
    }
  }
}
