{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "googlesheets/GridProperties",
  "title": "GridProperties",
  "x-openfn-adaptor": "googlesheets",
  "x-source": {
    "openapi": "#/components/schemas/GridProperties",
    "capturedAt": "2026-07-06"
  },
  "type": "object",
  "description": "Properties of a grid.",
  "properties": {
    "frozenRowCount": {
      "type": "integer",
      "format": "int32",
      "description": "The number of rows that are frozen in the grid."
    },
    "columnGroupControlAfter": {
      "type": "boolean",
      "description": "True if the column grouping control toggle is shown after the group."
    },
    "rowCount": {
      "type": "integer",
      "format": "int32",
      "description": "The number of rows in the grid."
    },
    "rowGroupControlAfter": {
      "type": "boolean",
      "description": "True if the row grouping control toggle is shown after the group."
    },
    "frozenColumnCount": {
      "type": "integer",
      "format": "int32",
      "description": "The number of columns that are frozen in the grid."
    },
    "hideGridlines": {
      "type": "boolean",
      "description": "True if the grid isn't showing gridlines in the UI."
    },
    "columnCount": {
      "type": "integer",
      "format": "int32",
      "description": "The number of columns in the grid."
    }
  }
}
