{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bigquery/SnapshotDefinition",
  "title": "SnapshotDefinition",
  "x-openfn-adaptor": "bigquery",
  "x-source": {
    "openapi": "#/components/schemas/SnapshotDefinition",
    "capturedAt": "2026-07-06"
  },
  "type": "object",
  "description": "Information about base table and snapshot time of the snapshot.",
  "properties": {
    "baseTableReference": {
      "$ref": "TableReference.json"
    },
    "snapshotTime": {
      "type": "string",
      "format": "date-time",
      "description": "Required. The time at which the base table was snapshot. This value is reported in the JSON response using RFC3339 format."
    }
  }
}
