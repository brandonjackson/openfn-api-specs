{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bigquery/EvaluationMetrics",
  "title": "EvaluationMetrics",
  "x-openfn-adaptor": "bigquery",
  "x-source": {
    "openapi": "#/components/schemas/EvaluationMetrics",
    "capturedAt": "2026-07-06"
  },
  "type": "object",
  "description": "Evaluation metrics of a model. These are either computed on all training data or just the eval data based on whether eval data was used during training. These are not present for imported models.",
  "properties": {
    "regressionMetrics": {
      "$ref": "RegressionMetrics.json"
    },
    "dimensionalityReductionMetrics": {
      "$ref": "DimensionalityReductionMetrics.json"
    },
    "binaryClassificationMetrics": {
      "$ref": "BinaryClassificationMetrics.json"
    },
    "arimaForecastingMetrics": {
      "$ref": "ArimaForecastingMetrics.json"
    },
    "rankingMetrics": {
      "$ref": "RankingMetrics.json"
    },
    "multiClassClassificationMetrics": {
      "$ref": "MultiClassClassificationMetrics.json"
    },
    "clusteringMetrics": {
      "$ref": "ClusteringMetrics.json"
    }
  }
}
