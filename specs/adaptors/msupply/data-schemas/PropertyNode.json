{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msupply/PropertyNode",
  "title": "PropertyNode",
  "x-openfn-adaptor": "msupply",
  "x-source": {
    "openapi": "#/components/schemas/PropertyNode",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "properties": {
    "__typename": {
      "type": "string",
      "description": "GraphQL type name of this object."
    },
    "id": {
      "type": "string"
    },
    "key": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "valueType": {
      "$ref": "PropertyNodeValueType.json"
    },
    "allowedValues": {
      "type": [
        "string",
        "null"
      ],
      "description": "If `valueType` is `String`, this field can contain a comma-separated list of allowed values, essentially defining an enum. If `valueType` is Integer or Float, this field will include the word `negative` if negative values are allowed."
    }
  }
}
