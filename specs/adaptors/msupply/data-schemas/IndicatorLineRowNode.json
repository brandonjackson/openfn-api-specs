{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msupply/IndicatorLineRowNode",
  "title": "IndicatorLineRowNode",
  "x-openfn-adaptor": "msupply",
  "x-source": {
    "openapi": "#/components/schemas/IndicatorLineRowNode",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "properties": {
    "__typename": {
      "type": "string",
      "description": "GraphQL type name of this object."
    },
    "id": {
      "type": "string"
    },
    "code": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "lineNumber": {
      "type": "integer",
      "format": "int32"
    },
    "valueType": {
      "anyOf": [
        {
          "$ref": "IndicatorValueTypeNode.json"
        },
        {
          "type": "null"
        }
      ],
      "description": "The line's own configured type, null where it declares none (`var` in mSupply). A CELL's type is its column's `valueType`, which already resolves the fallback to this one."
    },
    "isActive": {
      "type": "boolean"
    }
  }
}
