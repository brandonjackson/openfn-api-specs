{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msupply/IndicatorColumnNode",
  "title": "IndicatorColumnNode",
  "x-openfn-adaptor": "msupply",
  "x-source": {
    "openapi": "#/components/schemas/IndicatorColumnNode",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "properties": {
    "__typename": {
      "type": "string",
      "description": "GraphQL type name of this object."
    },
    "name": {
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "valueType": {
      "anyOf": [
        {
          "$ref": "IndicatorValueTypeNode.json"
        },
        {
          "type": "null"
        }
      ],
      "description": "The cell's effective type — this column's configured type, falling back to its line's where the column declares none (`var` in mSupply). This is the type an edit is validated against, so it is the one to render an input from. Null where neither declares a type: an edit is then not type-checked at all."
    },
    "columnNumber": {
      "type": "integer",
      "format": "int32"
    },
    "value": {
      "anyOf": [
        {
          "$ref": "IndicatorValueNode.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "isActive": {
      "type": "boolean"
    }
  }
}
