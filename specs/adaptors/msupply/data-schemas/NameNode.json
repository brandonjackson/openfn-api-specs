{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msupply/NameNode",
  "title": "NameNode",
  "x-openfn-adaptor": "msupply",
  "x-source": {
    "openapi": "#/components/schemas/NameNode",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "properties": {
    "__typename": {
      "type": "string",
      "description": "GraphQL type name of this object."
    },
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "code": {
      "type": "string"
    },
    "type": {
      "$ref": "NameNodeType.json"
    },
    "isCustomer": {
      "type": "boolean"
    },
    "isSupplier": {
      "type": "boolean"
    },
    "isVisible": {
      "type": "boolean"
    },
    "isSystemName": {
      "type": "boolean"
    },
    "store": {
      "anyOf": [
        {
          "$ref": "StoreNode.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "firstName": {
      "type": [
        "string",
        "null"
      ]
    },
    "lastName": {
      "type": [
        "string",
        "null"
      ]
    },
    "gender": {
      "anyOf": [
        {
          "$ref": "GenderTypeNode.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "phone": {
      "type": [
        "string",
        "null"
      ]
    },
    "chargeCode": {
      "type": [
        "string",
        "null"
      ]
    },
    "comment": {
      "type": [
        "string",
        "null"
      ]
    },
    "country": {
      "type": [
        "string",
        "null"
      ]
    },
    "address1": {
      "type": [
        "string",
        "null"
      ]
    },
    "address2": {
      "type": [
        "string",
        "null"
      ]
    },
    "email": {
      "type": [
        "string",
        "null"
      ]
    },
    "website": {
      "type": [
        "string",
        "null"
      ]
    },
    "isManufacturer": {
      "type": "boolean"
    },
    "isDonor": {
      "type": "boolean"
    },
    "isOnHold": {
      "type": "boolean"
    },
    "createdDatetime": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time"
    },
    "dateOfBirth": {
      "type": [
        "string",
        "null"
      ],
      "format": "date"
    },
    "customData": {
      "description": "Arbitrary JSON value."
    },
    "properties": {
      "type": "string",
      "description": "Returns a JSON string of the name properties e.g {\"property_key\": \"value\"}"
    },
    "customFields": {
      "description": "Properties v2 values for this name. The raw `name.custom_fields` JSONB blob is filtered server-side to keys that are (a) defined in `custom_field` and not soft-deleted, (b) marked visible for one of this name's table scopes via `custom_field_scope`. Stray keys never reach the client. A name has no single scope: \"customer\"/\"supplier\" are independent role flags (not mutually exclusive) and \"patient\" is a type, so the visible set is the **union** over every scope the name qualifies for — `\"patient\"` if it's a patient, `\"customer\"` if `is_customer`, `\"supplier\"` if `is_supplier`. A name that matches none of these (e.g. a manufacturer/donor/store-only name) has no scope and surfaces nothing."
    },
    "hshCode": {
      "type": [
        "string",
        "null"
      ]
    },
    "hshName": {
      "type": [
        "string",
        "null"
      ]
    },
    "margin": {
      "type": [
        "number",
        "null"
      ],
      "format": "double"
    },
    "freightFactor": {
      "type": [
        "number",
        "null"
      ],
      "format": "double"
    },
    "currency": {
      "anyOf": [
        {
          "$ref": "CurrencyNode.json"
        },
        {
          "type": "null"
        }
      ]
    }
  }
}
