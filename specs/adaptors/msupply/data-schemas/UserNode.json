{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msupply/UserNode",
  "title": "UserNode",
  "x-openfn-adaptor": "msupply",
  "x-source": {
    "openapi": "#/components/schemas/UserNode",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "properties": {
    "__typename": {
      "type": "string",
      "description": "GraphQL type name of this object."
    },
    "userId": {
      "type": "string",
      "description": "Internal user id"
    },
    "email": {
      "type": [
        "string",
        "null"
      ],
      "description": "The user's email address"
    },
    "username": {
      "type": "string"
    },
    "defaultStore": {
      "anyOf": [
        {
          "$ref": "UserStoreNode.json"
        },
        {
          "type": "null"
        }
      ]
    },
    "stores": {
      "$ref": "UserStoreConnector.json"
    },
    "permissions": {
      "$ref": "UserStorePermissionConnector.json"
    },
    "language": {
      "$ref": "LanguageTypeNode.json"
    },
    "firstName": {
      "type": [
        "string",
        "null"
      ]
    },
    "lastName": {
      "type": [
        "string",
        "null"
      ]
    },
    "phoneNumber": {
      "type": [
        "string",
        "null"
      ]
    },
    "jobTitle": {
      "type": [
        "string",
        "null"
      ]
    },
    "inactivityTimeoutSeconds": {
      "type": "integer",
      "format": "int32",
      "description": "How long (in seconds) the user may be inactive before the client should force a re-login. Sourced from server configuration (`server.inactivity_timeout_seconds`); advisory — the server does not enforce it."
    },
    "tokenRefreshIntervalSeconds": {
      "type": "integer",
      "format": "int32",
      "description": "If the user is active but no API call has happened for this long (in seconds), the client should call the refresh endpoint (`refreshToken`) to keep the session alive. Sourced from server configuration (`server.token_refresh_interval_seconds`)."
    }
  }
}
