{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msupply/SyncSettingsNode",
  "title": "SyncSettingsNode",
  "x-openfn-adaptor": "msupply",
  "x-source": {
    "openapi": "#/components/schemas/SyncSettingsNode",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "properties": {
    "__typename": {
      "type": "string",
      "description": "GraphQL type name of this object."
    },
    "url": {
      "type": "string",
      "description": "Central server url"
    },
    "username": {
      "type": "string",
      "description": "Central server username"
    },
    "intervalSeconds": {
      "type": "integer",
      "format": "int32",
      "description": "How frequently central data is synced"
    },
    "centralServerSiteId": {
      "type": "integer",
      "format": "int32"
    },
    "syncSiteId": {
      "type": [
        "integer",
        "null"
      ],
      "format": "int32"
    },
    "batchSize": {
      "type": [
        "integer",
        "null"
      ],
      "format": "int32",
      "description": "Configured sync batch size. Returns Some only when all three underlying values are equal and differ from the defaults, so the UI can pre-fill its single input. Non-uniform legacy values are reported as None."
    }
  }
}
