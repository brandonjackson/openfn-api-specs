{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "msupply/FrontendPluginMetadataNode",
  "title": "FrontendPluginMetadataNode",
  "x-openfn-adaptor": "msupply",
  "x-source": {
    "openapi": "#/components/schemas/FrontendPluginMetadataNode",
    "capturedAt": "2026-09-13"
  },
  "type": "object",
  "properties": {
    "__typename": {
      "type": "string",
      "description": "GraphQL type name of this object."
    },
    "code": {
      "type": "string"
    },
    "path": {
      "type": "string"
    },
    "hash": {
      "type": "string",
      "description": "Hash of the plugin's bundled file contents — clients append this as a cache-busting URL token (?v=...) so the browser only refetches when the bundle's bytes change."
    }
  }
}
