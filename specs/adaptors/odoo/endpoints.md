# Odoo External API (JSON-RPC model) (odoo) v16.0

<!-- derived from openapi.json by `pnpm specs index odoo` — do not edit by hand -->

base: https://demo.odoo.com | auth: not declared | 2 operations in 2 resources

Each line is `METHOD /path — operationId: summary`. For a request body, parameters or response shape, look the operationId up in `openapi.json`; for the objects an operation returns, see `data-schemas/index.json`.

## jsonrpc
- POST /jsonrpc — callObject: Call a model method (execute_kw)

## web
- POST /web/session/authenticate — authenticate: Authenticate a session
