{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "googlehealthcare/BulkExportGcsDestination",
  "title": "BulkExportGcsDestination",
  "x-openfn-adaptor": "googlehealthcare",
  "x-source": {
    "openapi": "#/components/schemas/BulkExportGcsDestination",
    "capturedAt": "2026-07-06"
  },
  "type": "object",
  "description": "The configuration for exporting to Cloud Storage using the bulk export API.",
  "properties": {
    "uriPrefix": {
      "type": "string",
      "description": "Optional. URI for a Cloud Storage directory where the server writes result files, in the format `gs://{bucket-id}/{path/to/destination/dir}`. If there is no trailing slash, the service appends one when composing the object path. The user is responsible for creating the Cloud Storage bucket referenced in `uri_prefix`."
    }
  }
}
